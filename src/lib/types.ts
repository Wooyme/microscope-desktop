export type Period = {
    id: string;
    name: string;
    description: string;
};
  
export type Event = {
    id:string;
    name: string;
    description: string;
};
  
export type Legacy = {
    id: string;
    source: string;
    target: string;
    description: string;
};
